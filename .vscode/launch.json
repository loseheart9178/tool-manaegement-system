{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C project",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/tool_manager",
            "args": [],
            "cwd": "${workspaceFolder}",
            "terminal": "integrated",
            "preLaunchTask": "Build C project"
        }
    ]
}
